#include <bits/stdc++.h>
using namespace std;


int main () {
    int t; cin >> t;
    while(t--){
        int n; cin >> n;
        vector<long long> h(n), a(n), t(n);
        
        for (int i = 0; i < n; i++)
        {
            cin >> h.at(i);
        }
        for (int i = 0; i < n; i++)
        {
            cin >> a.at(i);
        }
        for (int i = 0; i < n; i++)
        {
            cin >> t.at(i);
        }
        
        for (auto T : t) {
            
        }

    }


    return 0;
}